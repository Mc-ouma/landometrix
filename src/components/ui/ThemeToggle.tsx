'use client';

import React from 'react';

// This component doesn't need to change theme anymore since we only use dark theme
export default function ThemeToggle() {
  return null; // Don't render anything
}
