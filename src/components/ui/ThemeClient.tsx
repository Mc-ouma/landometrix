'use client';

import { useState, useEffect } from 'react';
import ThemeToggle from './ThemeToggle';

// This component is no longer needed since we don't have a theme toggle
export default function ThemeClient() {
  return null;
}
