'use client';

// This file exists only for backward compatibility with any code that might be importing it.
// The mobile menu functionality has been consolidated into the Header component.

import { FC } from 'react';

// Empty component to prevent import errors
const MobileMenu: FC = () => null;

export default MobileMenu;